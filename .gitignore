# 第三方词库源（wordbank-sources/）：各自独立 git clone，自带版本管理，不纳入本仓库
wordbank-sources/

# Claude Code 本地权限配置（机器相关）
.claude/settings.local.json

# 素材图副本（output 内，供 storyboard HTML 双击加载图，体积大不入库）
output/Material Collection/

# 素材图库原图（只跟踪素材清单索引，图片本身体积大不入库）
Material Collection/**/*.jpg
Material Collection/**/*.jpeg
Material Collection/**/*.png
Material Collection/**/*.webp
Material Collection/**/*.gif

# 素材缩略图（scan-assets.py 本地生成，靠 mtime 增量；不入库）
workspace/assets-thumb/
